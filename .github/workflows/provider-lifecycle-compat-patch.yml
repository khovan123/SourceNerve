name: Provider Lifecycle Compatibility Patch (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Provider lifecycle compatibility checkpoints are complete; this workflow is intentionally disabled."
